{% extends "../_layouts/base.njk" %}

{% block json_ld %}{{ jsonld | toJSONLDBlog | safe }}{% endblock %}

{% block content %}
  <div class="container">
    <div class="row">
      {{ content | safe }}

      <div class="tools">
        {%- for tool in collections.tools | reverse -%}
          <div class="tool">
            <div class="tool__body">
              <h3 class="tool__heading">
                <a href="{{ site.url }}/tools/{{ tool.data.uri }}/" class="tool__anchor">{{ tool.data.anchor }}</a>
              </h3>

              <div class="tool__description">
                <p>{{ tool.data.summary }}</p>
              </div>
            </div>
          </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
{% endblock %}

