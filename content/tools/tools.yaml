layout: tool
permalink: "tools/{{ uri | slug }}/index.html"
changeFreq: weekly
tags:
  - tools

jsonld:
  "@type": SoftwareApplication
  applicationCategory: DeveloperApplication

eleventyComputed:
  jsonld:
    name: "{{ anchor }}"
    description: "{{ summary }}"
