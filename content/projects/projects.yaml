layout: project
permalink: "projects/{{ uri | slug }}/index.html"
changeFreq: weekly
tags:
  - projects

jsonld:
  "@type": Article

eleventyComputed:
  jsonld:
    headline: "{{ heading }}"
    description: "{{ summary }}"
    mainEntityOfPage: "{{ site.url }}{{ page.url }}"
